{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "module": "index.ts",
  "scripts": {
    "build": "cd game && nuxt build",
    "dev": "cd game && nuxt dev",
    "preview": "cd game && nuxt preview",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "setup": "bun ./scripts/_setup.ts",
    "test": "bun run test:unit && bun run test:e2e",
    "setup:db": "bun ./scripts/setupSupabaseDB.ts"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.7.3",
    "@types/bun": "latest",
    "@types/decompress": "^4.2.7",
    "convert-csv-to-json": "^3.6.0",
    "decompress": "^4.2.1",
    "typescript": "~6.0.2"
  }
}
